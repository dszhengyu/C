DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
	MOV AX, 0B800H
	MOV DS, AX
	MOV SI, 160 * 12 + 36 * 2
	
    MOV AL, 9
    CALL CAL
	MOV BYTE PTR [SI], 2FH
	ADD SI, 2
	
	MOV AL, 8
	CALL CAL
	MOV BYTE PTR [SI], 2FH
	ADD SI, 2
	
	MOV AL, 7
	CALL CAL
	MOV BYTE PTR [SI], 20H
	ADD SI, 2
	
	MOV AL, 4
	CALL CAL
	MOV BYTE PTR [SI], 3AH
	ADD SI, 2
	
	MOV AL, 2
	CALL CAL
	MOV BYTE PTR [SI], 3AH
	ADD SI, 2
	
	MOV AL, 0
	CALL CAL
	
	
	    
    MOV AH,4CH
    INT 21H
    
CAL:
	OUT 70H, AL
    IN AL, 71H
    CALL TRANS
    MOV [SI], AH
    ADD SI, 2
    MOV [SI], AL
	ADD SI, 2
	RET
    
TRANS:
	MOV AH, AL
	MOV CL, 4
	SHR AH, CL
	AND AL, 00001111B
	ADD AH, 30H
	ADD AL, 30H
	RET
	
CODES ENDS
    END START
